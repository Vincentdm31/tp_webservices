import { ClientCacheInterceptor } from './client-cache.interceptor';

describe('ClientCacheInterceptor', () => {
  it('should be defined', () => {
    expect(new ClientCacheInterceptor()).toBeDefined();
  });
});
