import { commonResourceMatch } from './common-resource-match';

describe('commonResourceMatch', () => {
  it('should work', () => {
    expect(commonResourceMatch()).toEqual('common-resource-match');
  });
});
