import { commonResourceEquipe } from './common-resource-equipe';

describe('commonResourceEquipe', () => {
  it('should work', () => {
    expect(commonResourceEquipe()).toEqual('common-resource-equipe');
  });
});
