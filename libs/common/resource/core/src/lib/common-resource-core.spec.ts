import { commonResourceCore } from './common-resource-core';

describe('commonResourceCore', () => {
  it('should work', () => {
    expect(commonResourceCore()).toEqual('common-resource-core');
  });
});
